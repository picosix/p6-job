# import { User } from "User.graphql"

type Query {
  users: [User!]
}

type Mutation {
  createUser(
    email: String!
    username: String!
    status: Int
    password: String
    source: String
  ): User
}

schema {
  query: Query
  mutation: Mutation
}
